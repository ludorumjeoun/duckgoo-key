/target/
.DS_Store
/.idea/
/.vscode/
*.log
release-manifest.json

# Machine-local release settings; secrets live in macOS Keychain.
/mise.local.toml

# Signing private keys and temporary keychains must never enter the repository.
*.key
*.key.pem
*.p12
*.pfx
*.keychain
*.keychain-db
